spring:
  application:
    name: playground-spring-boot-admin
  boot:
    admin:
      discovery:
        ignored-services: consul

management:
  endpoints.web.exposure.include: "*"
  endpoint.health.show-details: always

server:
  port: 7473

